const PORT = 8082;
const EMPLOYEE_TYPES = ["admin", "tailor", "accounent"];
const DEFAULT_USER = "admin";
const DEFAULT_ADDRESSS = "ADDRESS_NOT_SET";
module.exports = {
  port: PORT,
  usertype: DEFAULT_USER,
  default_address: DEFAULT_ADDRESSS,
};
